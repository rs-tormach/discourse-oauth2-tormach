en:
  login:
    tor_authenticator_error_fetch_user_details: "Could not retrieve your user details. Do you have an active account?"

  site_settings:
    torauth2_enabled: "Custom OAuth2 is enabled"
    torauth2_client_id: "Client ID for custom OAuth2"
    torauth2_client_secret: "Client Secret for custom OAuth2"
    torauth2_authorize_url: "Authorization URL for OAuth2"
    torauth2_authorize_signup_url: '(optional) Alternative authorization URL used when the "Sign Up" button is used'
    torauth2_token_url: "Token URL for OAuth2"
    torauth2_token_url_method: "Method used to fetch the Token URL"
    torauth2_callback_user_id_path: "Path in the token response to the user id. eg: params.info.uuid"
    torauth2_callback_user_info_paths: "Paths in the token response to other user properties. Supported properties are name, username, email, email_verified and avatar. Format is property:path, eg: name:params.info.name"
    torauth2_fetch_user_details: "Fetch user JSON for OAuth2"
    torauth2_user_json_url: "URL to fetch user JSON for OAuth2 (note we replace :id with the id returned by OAuth call and :token with the token id)"
    torauth2_user_json_url_method: "Method used to fetch the user JSON URL"
    torauth2_json_user_id_path: "Path in the OAuth2 User JSON to the user id. eg: user.id"
    torauth2_json_username_path: "Path in the OAuth2 User JSON to the username. eg: user.username"
    torauth2_json_name_path: "Path in the OAuth2 User JSON to the user's full. eg: user.name.full"
    torauth2_json_email_path: "Path in the OAuth2 User JSON to the user's email. eg: user.email"
    torauth2_json_email_verified_path: "Path in the OAuth2 User JSON to the user's email verification state. eg: user.email.verified. torauth2_email_verified must be disabled for this setting to have any effect"
    torauth2_json_avatar_path: "Path in the Oauth2 User JSON to the user's avatar. eg: user.avatar_url"
    torauth2_email_verified: "Check this if the OAuth2 site has verified the email"
    torauth2_overrides_email: "Override the Discourse email with the remote email on every login. Works the same as the `auth_overrides_email` setting, but is specific to OAuth2 logins."
    torauth2_send_auth_header: "Send client credentials in an HTTP Authorization header"
    torauth2_send_auth_body: "Send client credentials in the request body"
    torauth2_debug_auth: "Include rich debugging information in your logs"
    torauth2_authorize_options: "When authorizing request these options"
    torauth2_scope: "When authorizing request this scope"
    torauth2_button_title: "The text for the OAuth2 button"
    torauth2_allow_association_change: Allow users to disconnect and reconnect their Discourse accounts from the OAuth2 provider
    torauth2_disable_csrf: "Disable CSRF check"

    errors:
      torauth2_fetch_user_details: "torauth2_callback_user_id_path must be present to disable torauth2_fetch_user_details"
